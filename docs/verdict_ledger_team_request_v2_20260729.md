# 판정원장 팀 확인 요청 【갱신판 v2 2026-07-29】

> **상태: 종결.** 항목별 채택값과 독립 재수집의 남은 범위는
> `verdict_ledger_final_decision_record_20260729.md`를 정본으로 본다.

판정원장 strict FINAL 전환 전 아래 **다섯 가지**를 확인해 주세요.
(1~3항은 기존 요청 그대로, 4~5항은 3차 독립 재검증에서 추가 —
근거: `판정원장_계보확정_및_재구축검증_20260729.md`)

## 1. 한미반도체 FY2019~FY2022 판정

현재 값은 `mem_ratio=0.80`, `process_confirmed=TRUE`,
`committee_ok=TRUE`이므로 규칙 C에 따라 위성군으로 편입됩니다.

- **안 A:** 현재 플래그 승인
  결과: FY2019~FY2022 위성군, FY2023부터 핵심군으로 이동
- **안 B:** FY2023 신규 편입이 의도
  조치: FY2019~FY2022의 공정 확인 또는 위원회 확인 플래그를 근거와 함께 정정

확인 문구:

> 한미반도체 FY2019~FY2022를 규칙 C 위성군으로 인정하는 것이 맞는지,
> 아니면 당시 공정 확인/위원회 확인 플래그를 FALSE로 정정해야 하는지 확인 부탁드립니다.

## 2. FY2025 정본 값 두 건

| 종목 | 필드 | 제출 원장 | 기존 확정 스냅샷 |
|---|---|---:|---:|
| 삼성전자(005930) | 메모리향 비율 | 31% | 55% |
| 인텍플러스(064290) | HBM 매출 노출도 | 0% | 2% |

확인 문구:

> 같은 FY2025 기준에서 두 파일의 값이 다릅니다. 각 값의 원천 공시,
> 산정식과 기준일을 확인해 정본을 지정해 주세요.

## 3. 관리종목 이력 승인

별도 확인 파일은 33종목 전부 N이며 확인자·확인일·근거 URL이 채워져 있습니다.

확인 문구:

> `관리종목이력_확인.csv`의 KIND 조회 방식과 무지정 근거 URL을
> strict FINAL의 증빙으로 승인하는지 확인 부탁드립니다.

## 4. 접수일(disclosed_at) 정본 기준 두 건 【추가】

215행 중 2행만 `disclosed_at`이 rcpNo 내장일과 다릅니다. 둘 다 하루 **늦은** 방향이라
룩어헤드는 아니며(보수적), 나머지 213행은 완전 일치합니다.

| 종목 | 사업연도 | disclosed_at | rcpNo (내장일) |
|---|---|---|---|
| 한솔케미칼(014680) | FY2020 | 2021-03-17 | `20210316001222` (03-16) |
| 하나마이크론(067310) | FY2022 | 2023-03-28 | `20230327000992` (03-27) |

확인 문구:

> `disclosed_at`은 DART API 재수집 결과의 `rcept_dt`(접수일)를 정본으로 사용하고,
> rcpNo 내장일은 진단값으로만 대조하는 원칙을 승인해 주세요. 위 2행은 고정 수집기로
> 재수집한 `rcept_dt`에 맞춰 FINAL 전에 정정하고 근거를 남기겠습니다.

## 5. 원본 유실·미확보 처리 및 재구축 검증 게이트 승인 【추가】

작업메모가 기술한 원본 산출물 7종(`collect_ledger.py`·`fill_ledger.py`의 EXPOSURE 판정표·
`evidence_pit/` 진본 카드·`ledger/*.csv`·`후보리스트`·`tests_scan.py`)은 현재 제공된
제출물·백업 ZIP·연결 작업공간에서 확보되지 않았습니다. 작성자 확인 기록을 함께 고려해
프로젝트 계보상 **원본 유실·미확보**로 처리합니다(계보확정 보고서 §1). 현재 `evidence_pit/` 카드는
원장에서 역생성한 재구성본으로 독립 증거가 아닙니다(순환 참조).

확인(승인) 문구:

> ① L09는 현재 **PARTIAL**로 유지하고, DART 재수집 215건 전수 대조
> (rcpNo·접수일·HBM 언급횟수) 통과 시 **METADATA_VERIFIED**로 올리는 것을 승인해 주세요.
> ② strict FINAL 게이트에 위 **재수집 전수 대조 통과를 필수 선행 조건으로 추가**하는 것을
> 승인해 주세요 (실행 도구: `rebuild_verification_kit_fixed_20260729.zip`).
> ③ 재구성 카드는 "재구성" 라벨 유지·감사 서류 진본 인용 금지, `fill_ledger.py`는
> `build_ledger_from_evidence.py` 동일본임을 명기하는 취급 원칙을 승인해 주세요.
> ④ 메타데이터 대조와 별도로 `hbm_exposure`, `mem_ratio`, `process_confirmed`,
> `committee_ok`의 원문 근거와 판단을 검토한 뒤에만 FINAL로 종결하는 것을 승인해 주세요.

실행 금지본은 구 `collect_fill_evidence_pit_20260729.zip`과 구
`재구축검증킷_20260729.zip`입니다. 두 구판의 수집기는 `annual_report()`에
`final=False`가 없어 정정공시가 있는 경우 룩어헤드를 재발시킬 수 있습니다.

## 문서 산식 정정

작성가이드의 `33종목×7년=223행`은 오기입니다. 이론 격자 231개에서 상장 전
8개 조합을 제외해 223행이며, 223행 중 별도의 8행이 `NO_DATA`입니다.
두 8개 집합은 서로 다릅니다. 정정본 XLSX에 반영했습니다.

---

위 다섯 항목이 닫히면 위원회가 유효 215행의 `DRAFT -> FINAL` 전환을 승인하고,
strict 빌드를 재실행하면 됩니다.

*갱신 이력: 2026-07-29 — v2에서 수집기 버전 사고, 계보 표현, 접수일 정본,
L09 단계 및 작성가이드 산식을 정정.*
